# 项目配置文件说明

## package.json - 项目配置文件

这是 Node.js 项目的核心配置文件，定义了项目的基本信息和依赖。

### 主要字段说明：

```json
{
  "name": "personal-portfolio",     // 项目名称
  "private": true,                   // 是否私有项目（不发布到 npm）
  "version": "0.0.0",                // 项目版本号
  "type": "module",                  // 使用 ES Module 模块系统（现代 JavaScript）
  
  // scripts：定义可以通过 npm run 执行的命令
  "scripts": {
    "dev": "vite",                   // 启动开发服务器（npm run dev）
    "build": "tsc && vite build",    // 构建生产版本（先检查类型，再构建）
    "preview": "vite preview"        // 预览构建后的项目
  },
  
  // dependencies：运行时需要的包（用户访问网站时需要）
  "dependencies": {
    "react": "^18.2.0",              // React 核心库（构建用户界面）
    "react-dom": "^18.2.0",          // React DOM 渲染库（将 React 渲染到网页）
    "lucide-react": "^0.294.0",      // 图标库（提供各种漂亮的图标）
    "framer-motion": "^10.16.16"     // 动画库（实现流畅的动画效果）
  },
  
  // devDependencies：开发时需要的包（只在写代码时用）
  "devDependencies": {
    "@types/react": "^18.2.43",      // React 的 TypeScript 类型定义
    "@types/react-dom": "^18.2.17",  // React DOM 的 TypeScript 类型定义
    "@vitejs/plugin-react": "^4.2.1",// Vite 的 React 插件
    "autoprefixer": "^10.4.16",      // CSS 自动添加浏览器前缀
    "postcss": "^8.4.32",            // CSS 处理工具
    "tailwindcss": "^3.3.6",         // Tailwind CSS 框架
    "typescript": "^5.2.2",          // TypeScript 语言
    "vite": "^5.0.8"                 // Vite 构建工具
  }
}
```

### 常用命令：

- `npm install` - 安装所有依赖包
- `npm run dev` - 启动开发服务器（本地调试）
- `npm run build` - 构建生产版本（准备发布）
- `npm run preview` - 预览构建结果

---

## tsconfig.json - TypeScript 配置文件

这个文件告诉 TypeScript 编译器如何处理代码。

### 主要配置项：

```json
{
  "compilerOptions": {
    "target": "ES2020",              // 编译目标：ES2020 版本的 JavaScript
    "lib": ["ES2020", "DOM"],        // 可用的库：ES2020 和浏览器 DOM
    "module": "ESNext",              // 模块系统：最新的 ES Module
    "jsx": "react-jsx",              // JSX 处理方式：React 的 JSX 语法
    
    "strict": true,                  // 严格模式：更严格的类型检查
    "noUnusedLocals": true,          // 检查未使用的变量
    "noUnusedParameters": true       // 检查未使用的参数
  }
}
```

---

## vite.config.ts - Vite 构建工具配置

Vite 是一个现代化的前端构建工具，速度非常快。

```typescript
import { defineConfig } from 'vite'  // 导入配置定义函数
import react from '@vitejs/plugin-react' // 导入 React 插件

export default defineConfig({
  plugins: [react()],                // 使用 React 插件
  server: {
    port: 50258                      // 开发服务器端口
  }
})
```

---

## tailwind.config.js - Tailwind CSS 配置

Tailwind CSS 是一个实用工具优先的 CSS 框架。

```javascript
export default {
  content: [
    "./index.html",                  // 扫描 HTML 文件
    "./src/**/*.{js,ts,jsx,tsx}",    // 扫描所有源代码文件
  ],
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'system-ui'], // 自定义字体
      },
      colors: {
        primary: { ... },             // 主色调（蓝色系）
        accent: { ... }               // 强调色（青色系）
      }
    }
  }
}
```

---

## 项目结构说明

```
Pro-PersonalPortfolio/
├── src/                    # 源代码目录
│   ├── components/         # React 组件目录
│   │   ├── Profile.tsx     # 个人介绍组件
│   │   ├── Skills.tsx      # 技能展示组件
│   │   └── Projects.tsx    # 项目展示组件
│   ├── App.tsx             # 主应用组件
│   ├── main.tsx            # 入口文件（启动应用）
│   ├── index.css           # 全局样式
│   ├── types.ts            # TypeScript 类型定义
│   └── data.ts             # 数据文件
├── index.html              # HTML 入口文件
├── package.json            # 项目配置
├── tsconfig.json           # TypeScript 配置
├── vite.config.ts          # Vite 配置
├── tailwind.config.js      # Tailwind CSS 配置
└── standalone.html         # 独立 HTML 版本（可直接打开）
```
